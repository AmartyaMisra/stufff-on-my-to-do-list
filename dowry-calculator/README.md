# Dowry Calculator (Parody Edition)

A satirical web app that mimics a serious finance calculator to expose the absurdity of dowry and societal biases. Built as a single static HTML file â€” no build tools, no backend.

> This is parody/meme content. Do not take it seriously.

## ðŸš€ Live Locally

- Open `index.html` in any modern browser (Chrome, Edge, Firefox, Safari).
- On Windows: double-click `index.html`.
- From terminal (PowerShell):
  ```powershell
  Invoke-Item .\index.html
  ```

## ðŸ“ Project Location

The code is saved here on your machine:
- Directory: `c:\Users\AMARTYA`
- Main file: `index.html`

You can move these files into a project folder or a Git repo as you prefer.

## âœ¨ Features

- Clean, fintech-style UI (serious tone, unserious logic)
- Full set of inputs: Age, Height, BMI, Education, Profession, Caste, Religion, Horoscope, NRI, Family Wealth, Assets, Random Traits
- Invoice-style breakdown with sarcastic labels and multipliers
- Random meme taxes/bonuses and easter eggs (e.g., "C++ pointers" bonus)
- Mobile-friendly, no dependencies

## ðŸ§  Core Logic Summary

- Base: â‚¹1,00,000
- Adds/subtracts: age, height, BMI, education, profession, horoscope, NRI, family wealth, assets
- Multiplies: caste Ã— religion Ã— skin tone
- Adds meme taxes/bonuses at the end

Final formula:
```
Final = (Base + sum(modifiers)) * (Caste * Religion * SkinTone) + MemeTaxes
```

## ðŸ§ª Quick Test Scenarios

- Dark skin + MBA + US NRI â†’ x0.7 + â‚¹1L + â‚¹5L
- Artist + Lower caste + Manglik â†’ should go very low/negative
- Doctor + Upper caste + Matching Kundali â†’ should be absurdly high

## ðŸ› ï¸ Development

All logic and UI are inside `index.html`. To edit:
- Open in VS Code/Cursor and modify the inline `<script>` and `<style>`.

## ðŸŒ Deploy to GitHub Pages

1. Create a new GitHub repo (e.g., `dowry-calculator-parody`).
2. Add the file and push:
   ```powershell
   git init
   git add index.html README.md
   git commit -m "feat: initial parody dowry calculator"
   git branch -M main
   git remote add origin https://github.com/<your-username>/dowry-calculator-parody.git
   git push -u origin main
   ```
3. Enable Pages:
   - GitHub repo â†’ Settings â†’ Pages
   - Source: `Deploy from a branch`
   - Branch: `main` / folder: `/ (root)`
4. Your site will be at `https://<your-username>.github.io/dowry-calculator-parody/`.

## ðŸ§¾ Legal/Content Note

- This is satire aimed at critiquing harmful social norms.
- No endorsement of discriminatory ideas.

## ðŸ” File Map

```
index.html   # Single-file web app (UI + CSS + JS)
README.md    # You are here
```

## ðŸ“¸ Screenshot (optional)

- Add a screenshot by dragging a PNG into the repo and linking it here.

---
Built for laughs and reflection. Share responsibly. ðŸ˜‚
